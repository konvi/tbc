package it.konvi.utils.tbc.rest;

import org.junit.Test;
import org.junit.After;
import org.junit.Before;
import org.mockito.Mockito;
import static org.junit.Assert.*;
import static org.mockito.Mockito.*;
import konvi.utils.tbc.rest.WorklogOverviewPageResource;
import konvi.utils.tbc.rest.WorklogOverviewPageResourceModel;
import org.apache.wink.client.Resource;
import org.apache.wink.client.RestClient;

public class WorklogOverviewPageResourceFuncTest {

    @Before
    public void setup() {

    }

    @After
    public void tearDown() {

    }

    @Test
    public void messageIsValid() {

//        String baseUrl = System.getProperty("baseurl");
//        String resourceUrl = baseUrl + "/rest/tbc-page/1.0/message";
//
//        RestClient client = new RestClient();
//        Resource resource = client.resource(resourceUrl);
//
//        WorklogOverviewPageResourceModel message = resource.get(WorklogOverviewPageResourceModel.class);
//
//        assertEquals("wrong message","Hello World",message.getMessage());
    }
}
